import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { TextArea } from '@equinor/fusion-react-textarea';

<Meta
  title="Input/TextArea"
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

export const Default = ({}) => {
  return <TextArea label="Default" />;
};

export const Disabled = ({}) => {
  return <TextArea label="Disabled" disabled />;
};

export const Rows = ({}) => {
  return <TextArea label="Multiple rows" rows={4} />;
};

export const Required = ({}) => {
  return <TextArea label="Required" required />;
};

export const Helper = ({}) => {
  return <TextArea label="Helper" helper="This helper shows when active!" />;
};

export const PersistentHelper = ({}) => {
  return <TextArea label="Persistent helper" helper="This helper is persistent!" helperpersistent />;
};

export const Counter = ({}) => {
  return <TextArea label="Character counter" helper="Helper Text" helperPersistent maxlength={10} charCounter />;
};

## TextArea

### Default

<Canvas>
  <Story name="Default">{Default.bind({})}</Story>
  <Story name="Disabled" args={{}}>
    {Disabled.bind({})}
  </Story>
</Canvas>

### Size

<Canvas>
  <Story name="Rows">{Rows.bind({})}</Story>
</Canvas>

### Validation

<Canvas>
  <Story name="Required" args={{}}>
    {Required.bind({})}
  </Story>
</Canvas>

### Helper

<Canvas>
  <Story name="Helper" args={{}}>
    {Helper.bind({})}
  </Story>
  <Story name="PersistentHelper" args={{}}>
    {PersistentHelper.bind({})}
  </Story>
</Canvas>

### Counter

<Canvas>
  <Story name="Counter" args={{}}>
    {Counter.bind({})}
  </Story>
</Canvas>
