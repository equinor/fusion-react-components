import { Meta, Story, Canvas, ArgsTable, Description } from '@storybook/addon-docs';

import Template from './TextArea.template';
import readme from '@equinor/fusion-react-textarea/README.md';

<Meta
  title="Input/TextArea"
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

<Description markdown={readme} />

## Playground

<Canvas>
  <Story name="Playground" parameters={{chromatic: { disableSnapshot: true }}} args={{label:'Playground'}} >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Playground" />

## SnapShots

export const Helper = ({}) => {
  return <Template label="Helper" helper="This helper shows when active!" />;
};

export const PersistentHelper = ({}) => {
  return <Template label="Persistent helper" helper="This helper is persistent!" helperpersistent />;
};

export const Counter = ({}) => {
  return <Template label="Character counter" helper="Helper Text" helperPersistent maxlength={10} charCounter />;
};

### Default

<Story name="Default">
  <Template label="Default" />
  <Template label="Default" value="Fusion"/>
  <br/>
  <br/>
  <Template label="Disabled" disabled />
  <Template label="Disabled" disabled value="Fusion"/>
</Story>

### Size

<Story name="Rows">
  <Template label="Multiple rows" rows={4} />
</Story>

### Validation

<Story name="Required">
  <Template label="Required" required />
</Story>

### Helper

<Story name="Helper">
  <Template label="Helper" helper="This helper shows when active!" />
  <Template label="Persistent helper" helper="This helper is persistent!" helperpersistent />
</Story>

### Counter

<Story name="Counter">
  <Template label="Character counter" helper="Helper Text" helperPersistent maxlength={10} charCounter />
  <Template label="Character counter" helper="Helper Text" helperPersistent maxlength={10} charCounter value="Fusion" />
</Story>
