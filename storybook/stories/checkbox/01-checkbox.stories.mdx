import { Meta, Story, Canvas, ArgsTable, Description } from '@storybook/addon-docs';

import Template from './Checkbox.template';

import readme from '@equinor/fusion-react-checkbox/README.md';

export const Container = ({ children}) => (
    <div style={{display: 'flex', flexFlow: 'column', gap: '10px'}}> 
      {children}
    </div>
);

<Meta 
  title="Input/Checkbox" 
  component={Template}
/>

<Description markdown={readme} />

## Playground

<Canvas>
  <Story name="Playground" args={{label:'Playground'}} >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Playground" />

## SnapShots

<Story name="Snapshots" parameters={{ chromatic: { disableSnapshot: false } }}>
  <div style={{display: 'flex', flexFlow: 'column', gap: '10px'}}>
    <h4>standard</h4>
    <div style={{display: 'flex', gap: '10px'}}>
      <Template />
      <Template checked/>
      <Template indeterminate/>
    </div>
    <h4>Disabled</h4>
    <div style={{display: 'flex', gap: '10px'}}>
      <Template disabled />
      <Template disabled checked/>
      <Template disabled indeterminate/>
    </div>
  </div>
</Story>
