import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import Template from './CheckBoxTemplate';

import { Checkbox, CheckboxProps } from '@equinor/fusion-react-checkbox';

export const Container = ({ children}) => (
    <div style={{display: 'flex', flexFlow: 'column', gap: '10px'}}> 
      {children}
    </div>
);

<Meta 
  title="Input/Checkbox" 
  component={Template}
/>

# Checkbox

```sh
npm i @equinor/fusion-react-checkbox
```

<Canvas>
  <Story name="demo" parameters={{chromatic: { disableSnapshot: true }}}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="demo" />

## SnapShots

<Story name="snapshots">
  <div style={{display: 'flex', flexFlow: 'column', gap: '10px'}}>
    <h4>standard</h4>
    <div style={{display: 'flex', gap: '10px'}}>
      <Template />
      <Template checked/>
      <Template indeterminate/>
    </div>
    <h4>Disabled</h4>
    <div style={{display: 'flex', gap: '10px'}}>
      <Template disabled />
      <Template disabled checked/>
      <Template disabled indeterminate/>
    </div>
  </div>
</Story>
