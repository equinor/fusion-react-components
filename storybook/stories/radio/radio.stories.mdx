import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { Radio } from '@equinor/fusion-react-radio';

<Meta
  title="Input/Radio"
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

export const Default = ({}) => {
  return (
    <>
      <Radio name="my-group" value="value1" />
      <Radio name="my-group" value="value2" checked />
    </>
  );
};

export const Disabled = ({}) => {
  return (
    <>
      <Radio name="my-group-disabled" value="value1" disabled />
      <Radio name="my-group-disabled" value="value2" checked disabled />
    </>
  );
};

## Radio

### Default

<Canvas>
  <Story name="default" args={{}}>
    {Default.bind({})}
  </Story>
</Canvas>

### Disabled

<Canvas>
  <Story name="disabled" args={{}}>
    {Disabled.bind({})}
  </Story>
</Canvas>
