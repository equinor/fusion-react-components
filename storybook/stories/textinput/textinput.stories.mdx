import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import TextInput from '@equinor/fusion-react-textinput';

<Meta
  title="Input/TextInput"
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

export const Default = ({}) => {
  return <TextInput label="Default" />;
};

export const Required = ({}) => {
  return <TextInput label="Required" required />;
};

export const Pattern = ({}) => {
  return (
    <TextInput
      label="Pattern"
      pattern="[0-9]"
      value="1337"
      validationMessage="Input must be a number between 0-9!"
      autoValidate
      validateOnInitialRender
    />
  );
};

export const Disabled = ({}) => {
  return <TextInput label="Disabled" disabled />;
};

export const Helper = ({}) => {
  return <TextInput label="Helper" helper="This helper shows when active!" />;
};

export const PersistentHelper = ({}) => {
  return <TextInput label="Persistent helper" helper="This helper is persistent!" helperpersistent />;
};

export const Icon = ({}) => {
  return <TextInput label="Icon" icon="settings" />;
};

export const IconTrailing = ({}) => {
  return <TextInput label="Trailing icon" iconTrailing="settings" />;
};

export const Counter = ({}) => {
  return <TextInput label="Character counter" helper="Helper Text" helperPersistent maxlength="10" charCounter />;
};

## TextInput

### Default

<Canvas>
  <Story
    name="Default"
    args={{
      validityTransform: (message, nativeValidity) => {
        return { valid: false };
      },
    }}
  >
    {Default.bind({})}
  </Story>
  <Story name="Disabled" args={{}}>
    {Disabled.bind({})}
  </Story>
</Canvas>

### Validation

<Canvas>
  <Story name="Required" args={{}}>
    {Required.bind({})}
  </Story>
  <Story name="Pattern" args={{}}>
    {Pattern.bind({})}
  </Story>
</Canvas>

### Helper

<Canvas>
  <Story name="Helper" args={{}}>
    {Helper.bind({})}
  </Story>
  <Story name="PersistentHelper" args={{}}>
    {PersistentHelper.bind({})}
  </Story>
</Canvas>

### Icon

<Canvas>
  <Story name="Icon" args={{}}>
    {Icon.bind({})}
  </Story>
  <Story name="IconTrailing" args={{}}>
    {IconTrailing.bind({})}
  </Story>
</Canvas>

### Counter

<Canvas>
  <Story name="Counter" args={{}}>
    {Counter.bind({})}
  </Story>
</Canvas>
