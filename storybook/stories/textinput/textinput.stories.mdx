import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { TextInput, TextInputProps } from '@equinor/fusion-react-textinput';
import { iconNames } from '@equinor/fusion-wc-icon';

import { Template } from './Template';

<Meta
  title="Input/TextInput"
  parameters={{
    viewMode: 'docs',
  }}
  argTypes={{
    value: {
      control: 'text',
      description: 'set value of element',
    },
    helper: {
      control: 'text',
    },
    pattern: {
      control: 'text',
    },
    validationMessage: { 
      control: 'text' 
    },
    required: { 
      control: 'boolean' 
    },
    disabled: { 
      control: 'boolean' 
    },
    helperPersistent: { 
      control: 'boolean' 
    },
    autoValidate: { 
      control: 'boolean' 
    },
    validateOnInitialRender: { 
      control: 'boolean' 
    },
    icon: {
      control: 'select',
      options: iconNames,
    },
    maxlength: { 
      control: 'number' 
    },
    charCounter: { 
      control: 'boolean' 
    },
    onChange: {
      action: 'onChange',
    },
  }}
/>

# TextInput

```sh
npm i -D @equinor/fusion-react-textinput
```

```tsx
import { TextInput, TextInputChangeEvent } from '@equinor/fusion-react-textinput';
const Component = ({value}: ComponentProps) => {
  const [txt, setTxt] = useState(value);
  const onChange = useCallback(
    (e: TextInputChangeEvent) => setTxt(e.target.value ?? ''),
    [setTxt]
  );
  return (
    <div>
      <TextInput onChange={onChange} />
      <span>Value: {txt}</span>
    </div>
  );
};
```

### Default


<Canvas>
  <Story name="Default" args={{
      label: 'Default',
      validityTransform: (message, nativeValidity) => {
        return { valid: false };
      },
  }}>
    {Template.bind({})}
  </Story>
  <Story name="Disabled" args={{
    label: 'Disabled', 
    disabled: true
  }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Validation

<Canvas>
  <Story name="Required" args={{
    label: 'Required', 
    required: true
  }}>
    {Template.bind({})}
  </Story>
  <Story name="Pattern" args={{
      label: 'Pattern', 
      required: true, 
      pattern: "[0-9]",
      value:"1337",
      validationMessage:"Input must be a number between 0-9!",
      autoValidate: true,
      validateOnInitialRender: true 
  }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Helper

<Canvas>
  <Story name="Helper" args={{
    label:"Helper", 
    helper:"This helper shows when active!" 
  }}>
    {Template.bind({})}
  </Story>
  <Story name="PersistentHelper" args={{
    label:"PersistentHelper", 
    helper:"This helper shows when active!", 
    helperPersistent: true
  }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Icon

<Canvas>
  <Story name="Icon" args={{
    label: "Icon",
    icon: "settings"
  }}>
    {Template.bind({})}
  </Story>
  <Story name="Trailing icon" args={{
    label: "Trailing icon",
    iconTrailing: "settings",
  }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Counter

<Canvas>
  <Story name="Counter" args={{
      label:"Character counter", 
      maxlength:10,
      charCounter: true,
      helper:"Helper Text", 
      helperPersistent: true, 
  }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Propperties
<ArgsTable of={Template} />
