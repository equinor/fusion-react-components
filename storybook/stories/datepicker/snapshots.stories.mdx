import { Meta, Story, Canvas } from '@storybook/addon-docs';
import Template from './DatePicker.template';

import { FusionDatePicker } from '@equinor/fusion-react-datepicker';

<Meta 
  title="Snapshots/DatePicker" 
  component={Template}
/>


<Canvas>
  <div style={{height: 300, width: '100vw'}} />
  <Story name="Dates" args={{
    label: 'Date',
    placeholder: 'Select date',
    type: 'date',
  }}>
    {Template.bind({})}
  </Story>
  <Story name="DateRange" args={{
    label: 'Date range',
    placeholder: 'Select date range (from-to)',
    type: 'date-range',
  }}>
    {Template.bind({})}
  </Story>
  <Story name="DateAndTime" args={{
    label: 'Date and time',
    placeholder: 'Select date and time',
    type: 'datetime',
  }}>
    {Template.bind({})}
  </Story>
  <Story name="Month" args={{
    label: 'Month',
    placeholder: 'Select month',
    type: 'month',
  }}>
    {Template.bind({})}
  </Story>
  <Story name="Time" args={{
    label: 'Time',
    placeholder: 'Select time',
    type: 'time',
  }}>
    {Template.bind({})}
  </Story>
  <Story name="Year" args={{
    label: 'Year',
    placeholder: 'Select year',
    type: 'year',
  }}>
    {Template.bind({})}
  </Story>
  <Story name="Popper" args={{
    label: 'Popper',
    popperPlacement: 'top-end',
    popperModifiers: [
    {
      name: 'offset',
      options: { offset: [-20, -20] },
    },
  ]
  }}>
    {Template.bind({})}
  </Story>
  <br/>
  <div style={{height: 300, width: '100vw'}} />
</Canvas>