import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { DateRange } from '@equinor/fusion-react-date';

<Meta
  title="Data/DateRange"
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

export const Default = ({}) => {
  return (
    <>
      <DateRange name="my-group" value="value1" />
    </>
  );
};

## DateRange

### Default

<Canvas>
  <Story name="default" args={{}}>
    {Default.bind({})}
  </Story>
</Canvas>
