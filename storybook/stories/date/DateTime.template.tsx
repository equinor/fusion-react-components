import { DateTime, DateTimeProps } from '@equinor/fusion-react-date';

export const DateTimeTemplate = (props: DateTimeProps): JSX.Element => <DateTime {...props} />;

export default DateTimeTemplate;
