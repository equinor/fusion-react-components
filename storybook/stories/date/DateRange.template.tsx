import { DateRange, DateRangeProps } from '@equinor/fusion-react-date';

export const DateRangeTemplate = (props: DateRangeProps): JSX.Element => <DateRange {...props} />;

export default DateRangeTemplate;
