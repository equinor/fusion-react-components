import TextInput, { TextInputProps } from '@equinor/fusion-react-textinput';

export const Template = (props: TextInputProps): React.ReactElement => <TextInput {...props} />;

export default Template;
