import { Meta, Story, Canvas } from '@storybook/addon-docs';
import Chip from '@equinor/fusion-react-chip';

export const Container = ({ title, children }) => (
  <section>
    <h4>{title}</h4>
    <div style={{ display: 'flex', gap: '10px' }}>{children}</div>
  </section>
);

export const ChipTemplate = (props) => (
  <div>
    <Container title="Sizes">
      <Chip size="small" {...props}>
        Small
      </Chip>
      <Chip size="medium" {...props}>
        Medium
      </Chip>
      <Chip size="large" {...props}>
        Large
      </Chip>
    </Container>
    <Container title="Colors">
      <Chip color="primary" {...props}>
        Primary
      </Chip>
      <Chip color="secondary" {...props}>
        Secondary
      </Chip>
      <Chip color="success" {...props}>
        Success
      </Chip>
      <Chip color="danger" {...props}>
        Danger
      </Chip>
      <Chip color="warning" {...props}>
        Warning
      </Chip>
      <Chip color="disabled" {...props}>
        Disabled
      </Chip>
    </Container>
    <Container title="Icon">
      <Chip icon="settings" {...props}>
        Icon
      </Chip>
    </Container>
    <Container title="Clickable">
      <Chip clickable {...props}>
        Clickable
      </Chip>
    </Container>
    <Container title="Removable">
      <Chip removable {...props}>
        Removable
      </Chip>
    </Container>
    <Container title="Disabled">
      <Chip disabled {...props}>
        Disabled
      </Chip>
    </Container>
  </div>
);

<Meta title="Snapshots/Chip" parameters={{ chromatic: { disableSnapshot: false } }} />

<Story name="Filled" args={{ name: 'filled' }}>
  {ChipTemplate.bind({})}
</Story>

<Story name="Outlined" args={{ name: 'outlined', variant: 'outlined' }}>
  {ChipTemplate.bind({})}
</Story>
