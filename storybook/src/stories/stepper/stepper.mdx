import { Meta, Markdown, ArgTypes, Canvas } from '@storybook/blocks';

import readme from '#packages/stepper/README.md?raw';
import pkg from '#packages/stepper/package.json';

import changelogReact from '#packages/stepper/CHANGELOG.md?raw';

import { ChangeLog, PackageInfo } from '../../components';

import * as stories from './stepper.stories';


<Meta of={stories} />

<PackageInfo pkg={pkg} />

<Markdown>{readme}</Markdown>

<ArgTypes of={ stories.basicUncontrolled } />

## Basic Uncontrolled
<Canvas of={ stories.basicUncontrolled } meta={stories} />

## Vertical Uncontrolled
<Canvas of={ stories.verticalUncontrolled } meta={stories} />

## Controlled
<Canvas of={ stories.controlled } meta={stories} />

## Controlled With External Buttons
<Canvas of={ stories.controlledWithExternalButtons } meta={stories} />

<ChangeLog
  changelogs={{
    react: changelogReact,
  }}
/>
