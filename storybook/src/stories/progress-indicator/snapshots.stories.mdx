import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { CircularProgress, DotsProgress } from '@equinor/fusion-react-progress-indicator';

export const Container = ({ title, children }) => (
  <section>
    <h4>{title}</h4>
    <div
      style={{
        display: 'flex',
        gap: '2rem',
        padding: '1rem',
        backgroundColor: '#F7F7F7',
        borderRadius: '4px',
      }}
    >
      {children}
    </div>
  </section>
);

export const CircularTemplate = (props) => (
  <div>
    <h3>Circular</h3>
    <Container title="Colors">
      <CircularProgress size="medium" color="primary" {...props}>
        Primary
      </CircularProgress>
      <CircularProgress size="medium" color="neutral" {...props}>
        Neutral
      </CircularProgress>
    </Container>
    <Container title="Sizes">
      <CircularProgress color="primary" size="x-small" {...props}>
        X Small
      </CircularProgress>
      <CircularProgress color="primary" size="small" {...props}>
        Small
      </CircularProgress>
      <CircularProgress color="primary" size="medium" {...props}>
        Medium
      </CircularProgress>
      <CircularProgress color="primary" size="large" {...props}>
        Large
      </CircularProgress>
      <CircularProgress color="primary" size="x-large" {...props}>
        X Large
      </CircularProgress>
    </Container>
  </div>
);

export const DotsTemplate = (props) => (
  <div>
    <h3>Dots</h3>
    <Container title="Colors">
      <DotsProgress size="small" color="primary" {...props}>
        Primary
      </DotsProgress>
      <DotsProgress size="small" color="tertiary" {...props}>
        Neutral
      </DotsProgress>
      <DotsProgress size="small" color="neutral" {...props}>
        Neutral
      </DotsProgress>
    </Container>
    <Container title="Sizes">
      <DotsProgress color="primary" size="small" {...props}>
        Small
      </DotsProgress>
      <DotsProgress color="primary" size="medium" {...props}>
        Medium
      </DotsProgress>
      <DotsProgress color="primary" size="large" {...props}>
        Large
      </DotsProgress>
    </Container>
  </div>
);

<Meta title="Snapshots/ProgressIndicator" parameters={{ chromatic: { disableSnapshot: false } }} />

<Story name="Circular">{CircularTemplate.bind({})}</Story>

<Story name="Dots">{DotsTemplate.bind({})}</Story>
