import { Meta, Story, Canvas } from '@storybook/addon-docs';
import Checkbox from '@equinor/fusion-react-checkbox';

export const Container = ({ children}) => (
    <div style={{display: 'flex', flexFlow: 'column', gap: '10px'}}> 
      {children}
    </div>
);

<Meta title="Snapshots/Checkbox" parameters={{ chromatic: { disableSnapshot: false } }}/>

<Canvas>
  <Story name="Checkbox">
    <Container>
      <h4>standard</h4>
      <div style={{display: 'flex', gap: '10px'}}>
        <Checkbox />
        <Checkbox checked/>
        <Checkbox indeterminate/>
      </div>
      <h4>Disabled</h4>
      <div style={{display: 'flex', gap: '10px'}}>
        <Checkbox disabled />
        <Checkbox disabled checked/>
        <Checkbox disabled indeterminate/>
      </div>
    </Container>
  </Story>
</Canvas>
