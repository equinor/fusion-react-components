import { Meta, Markdown, ArgTypes, Canvas } from '@storybook/addon-docs/blocks';

import readme from '#packages/textarea/README.md?raw';
import pkg from '#packages/textarea/package.json';

import changelogReact from '#packages/textarea/CHANGELOG.md?raw';

import { ChangeLog, PackageInfo } from '../../components';

import * as stories from './textarea.stories';

<Meta of={stories} />

<PackageInfo pkg={pkg} />

<Markdown>{readme}</Markdown>

<ArgTypes of={ stories } />

### Basic
<Canvas of={ stories.basic } meta={stories}/>

### Sizes
<Canvas of={ stories.size } meta={stories}/>

### Required
<Canvas of={ stories.required } meta={stories}/>

### Disabled
<Canvas of={ stories.disabled } meta={stories}/>

### With counter
<Canvas of={ stories.charCounter } meta={stories}/>

### With help text
<Canvas of={ stories.withHelpText } meta={stories}/>

<ChangeLog
  changelogs={{
    react: changelogReact,
  }}
/>
