import TextArea, { TextAreaProps } from '@equinor/fusion-react-textarea';

export const TextAreaTemplate = (props: TextAreaProps): JSX.Element => <TextArea {...props} />;

export default TextAreaTemplate;
