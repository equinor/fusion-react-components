import { Meta, Markdown, Canvas } from '@storybook/addon-docs/blocks';

import readme from '#packages/styles/README.md?raw';
import pkg from '#packages/styles/package.json';
import changelogReact from '#packages/styles/CHANGELOG.md?raw';

import { ChangeLog, PackageInfo } from '../../components';

import * as scopeIsolationStories from './scope-isolation.stories';
import * as basicUsageStories from './basic-usage.stories';
import * as advancedFeaturesStories from './advanced-features.stories';
import * as themeSystemStories from './theme-system.stories';
import * as utilitiesStories from './utilities.stories';


<Meta title="ui/Styles" />

<PackageInfo pkg={pkg} />

<Markdown>{readme}</Markdown>

---

## Basic Usage

Get started with styling components using `makeStyles`. These examples show the fundamental patterns.

### Basic Styles
<Canvas of={basicUsageStories.BasicStyles} meta={basicUsageStories} />

Define styles as JavaScript objects and get CSS class names back.

### Dynamic Styles
<Canvas of={basicUsageStories.DynamicStyles} meta={basicUsageStories} />

Styles change based on component props passed to the hook.

### Theme-Based Styles
<Canvas of={basicUsageStories.ThemeBasedStyles} meta={basicUsageStories} />

Styles access theme values from ThemeProvider.

---

## Scope Isolation

Critical for micro-frontends and dynamically loaded modules. Demonstrates how to prevent CSS class name collisions using `StylesProvider` with different seeds.

### Nested Scopes
<Canvas of={scopeIsolationStories.NestedScopes} meta={scopeIsolationStories} />

Demonstrates nested StylesProviders with different seeds creating isolated class names from the same styles object.

### Side By Side Scopes
<Canvas of={scopeIsolationStories.SideBySideScopes} meta={scopeIsolationStories} />

Shows sibling StylesProviders at the same level, perfect for dynamically loaded modules or micro-frontends.

### Without Seed
<Canvas of={scopeIsolationStories.WithoutSeed} meta={scopeIsolationStories} />

Shows the default behavior when no seed is provided.

---

## Advanced Features

Powerful CSS features and performance optimizations available in the styling system.

### Nested Selectors
<Canvas of={advancedFeaturesStories.NestedSelectors} meta={advancedFeaturesStories} />

Use CSS features like `:hover`, `:focus`, and nested child selectors.

### Multiple Style Rules
<Canvas of={advancedFeaturesStories.MultipleStyleRules} meta={advancedFeaturesStories} />

One component can have multiple style classes.

### Style Caching
<Canvas of={advancedFeaturesStories.StyleCaching} meta={advancedFeaturesStories} />

Multiple components using the same styles share the same CSS stylesheet.

---

## Theme System

Working with themes, including composition and accessing theme values.

### Theme Composition
<Canvas of={themeSystemStories.ThemeComposition} meta={themeSystemStories} />

Nested ThemeProviders can extend or override parent themes.

### useTheme Hook
<Canvas of={themeSystemStories.UseThemeHook} meta={themeSystemStories} />

Hook to access theme from ThemeProvider context.

---

## Utilities

Helper functions and utilities for creating styles.

### createStyles Helper
<Canvas of={utilitiesStories.CreateStylesHelper} meta={utilitiesStories} />

Type-safe helper for creating style rules.

---

<ChangeLog
  changelogs={{
    react: changelogReact,
  }}
/>
