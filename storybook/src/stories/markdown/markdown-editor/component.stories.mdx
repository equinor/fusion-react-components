import { Meta, Story, Canvas, ArgsTable, Description } from '@storybook/addon-docs';
import { ChangeLog, PackageInfo, StoryExample } from '../../../components';
import { MarkdownEditor } from '@equinor/fusion-react-markdown/src';

<Meta title="Input/Markdown/Editor" component={MarkdownEditor} />

<PackageInfo pkg={require('@equinor/fusion-react-markdown/package.json')} />

# Markdown Editor

## Properties/Attributes
| Name | Type | Default | Description
| ---- | ---- | ------- | -----------
| `menuItems` | `Array<MdMenuItemType>*` | `['strong', 'em', 'bullet_list', 'ordered_list']` | List of visible menu buttons
| `minHeight` | `string` | `''` | Markdown Editor minimum height
| `value` | `string` | `''` | Markdown editors value
| `menuSize` | `MenuSizes**` | `'medium'` | Size of the menu buttons

---

\*  `Array<MdMenuItemType>` is list of showing visible menu buttons available as `MdMenuItemType`.
```ts
type MdMenuItemType =
  | 'strong'
  | 'em'
  | 'link'
  | 'ordered_list'
  | 'bullet_list'
  | 'paragraph'
  | 'blockquote'
  | 'h1'
  | 'h2'
  | 'h3';
```

\*  `MenuSizes` type imported from markdown component.
```ts
type MenuSizes = 'small' | 'medium' | 'large';
```

## Example Usage - Initial value as Child

```tsx
import { MarkdownEditor } from '@equinor/fusion-react-markdown';

<MarkdownEditor props>
  **some** markdown *text*
</MarkdownEditor>
```

## Example Usage - Initial value as Value
```jsx 
import { MarkdownEditor } from '@equinor/fusion-react-markdown';

const markdown = "# My Heading";
<MarkdownEditor value={markdown} onInput={(e)=>{console.log(e.target._value)}} change={console.log} />
```

## Example

<StoryExample storyId="examples-markdown-editor--component" showSource />

## Changelog

<ChangeLog changelogs={require('@equinor/fusion-react-markdown/CHANGELOG.md')} />