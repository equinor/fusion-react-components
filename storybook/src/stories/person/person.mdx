import { Meta, Markdown, ArgTypes, Canvas } from '@storybook/addon-docs/blocks';

import readme from '#packages/person/README.md?raw';
import pkg from '#packages/person/package.json';

import changelogReact from '#packages/person/CHANGELOG.md?raw';

import { ChangeLog, PackageInfo } from '../../components';

import * as avatarStories from './avatar/avatar.stories';
import * as cardStories from './card.stories';
import * as selectStories from './select.stories';
import * as cellStories from './cell.stories';
import * as peoplePickerStories from './people/people-picker.stories';
import * as peopleViewerStories from './people/people-viewer.stories';
import * as personPickerStories from './people/person-picker.stories';

<Meta title="Person" />

<PackageInfo pkg={pkg} />

<Markdown>{readme}</Markdown>

## Avatar
<ArgTypes of={ avatarStories.basic } />

### Basic
<Canvas of={ avatarStories.basic } meta={avatarStories} story={{height: '350px'}}/>

### Sizes
<Canvas of={ avatarStories.sizes } meta={avatarStories} story={{height: '350px'}}/>

### withDataSource

When providing the `dataSource` property with a full `PersonInfo` object, the avatar will not resolve any data about the user.
If the `PersonInfo` object is missing the `avatarUrl` property, the `avatarUrl` will still be resolved from the `PersonProvider`.

<Canvas of={ avatarStories.withDataSource } meta={avatarStories}  story={{height: '350px'}}/>

### Clickable
<Canvas of={ avatarStories.clickable } meta={avatarStories} story={{height: '350px'}}/>

### Trigger none
<Canvas of={ avatarStories.disableCard } meta={avatarStories}/>

### disabled
<Canvas of={ avatarStories.disabled } meta={avatarStories} />

## Card

<ArgTypes of={ cardStories.basic } />

### Basic
<Canvas of={ cardStories.basic } meta={cardStories} />

### Sizes
<Canvas of={ cardStories.sizes } meta={cardStories} />

### With data-source
<Canvas of={ cardStories.withDataSource } meta={cardStories} />

## Select

<ArgTypes of={ selectStories } />

### Basic
<Canvas of={ selectStories.basic } meta={selectStories} story={{height: '350px'}}/>

### Preserving output
<Canvas of={ selectStories.custom } meta={selectStories} story={{height: '350px'}}/>

## Cell
<ArgTypes of={ cellStories } />

### Basic
<Canvas of={ cellStories.basic } meta={cellStories} />

### Show Avatar
<Canvas of={ cellStories.showAvatar } meta={cellStories} />

### Custom Headings
<Canvas of={ cellStories.customHeading } meta={cellStories} />

### Sizes
<Canvas of={ cellStories.sizes } meta={cellStories} />

## PeoplePicker
<ArgTypes of={ peoplePickerStories } />

### Basic
<Canvas of={ peoplePickerStories.basic } meta={peoplePickerStories} />

## PeopleViewer
<ArgTypes of={ peopleViewerStories } />

### Basic
<Canvas of={ peopleViewerStories.basic } meta={peopleViewerStories} />

## PersonPicker
<ArgTypes of={ personPickerStories.basic } />

### Basic
<Canvas of={ personPickerStories.basic } meta={personPickerStories} />

<ChangeLog
  changelogs={{
    react: changelogReact,
  }}
/>
