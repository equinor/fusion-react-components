import { Meta, Markdown, ArgTypes, Canvas } from '@storybook/addon-docs/blocks';
import * as avatarStories from './avatar.stories';

<Meta title="Person/Avatar" />

## Avatar
<ArgTypes of={ avatarStories.basic } meta={avatarStories} />

### Basic
<Canvas of={ avatarStories.basic } meta={avatarStories} story={{height: '350px'}}/>

### Sizes
<Canvas of={ avatarStories.sizes } meta={avatarStories} story={{height: '350px'}}/>

### withDataSource

When providing the dataSource property with a PersonInfo object including a valid avatarUrl, the avatar will not resolve any data about the user.
If the PersonInfo object is missing avatarUrl property, the avatarUrl will still be resolved from the PersonProvider.

<Canvas of={ avatarStories.withDataSource } meta={avatarStories}  story={{height: '350px'}}/>

### Trigger click
<Canvas of={ avatarStories.TriggerClick } meta={avatarStories} story={{height: '350px'}}/>

### Trigger none
<Canvas of={ avatarStories.TriggerNone } meta={avatarStories}/>

### disabled
<Canvas of={ avatarStories.disabled } meta={avatarStories} />
