import { Meta, Story } from '@storybook/addon-docs';
import { theme } from '@equinor/fusion-react-styles';

import { Typography } from '@equinor/fusion-react-typography';

<Meta title="Snapshots/Typography" parameters={{ chromatic: { disableSnapshot: false } }} />

export const Container = ({ title, children }) => (
  <section>
    <div
      style={{
        display: 'flex',
        flexDirection: 'column',
        gap: '2rem',
        padding: '1rem',
        backgroundColor: '#F7F7F7',
        borderRadius: '4px',
      }}
    >
      {children}
    </div>
  </section>
);

### Heading

<Story name="Heading">
  {(args) => (
    <Container>
      <Typography variant="heading" type="h1_bold">
        Heading/H1 Bold
      </Typography>
      <Typography variant="heading" type="h1">
        Heading/H1
      </Typography>
      <Typography variant="heading" type="h2">
        Heading/H2
      </Typography>
      <Typography variant="heading" type="h3">
        Heading/H3
      </Typography>
      <Typography variant="heading" type="h4">
        Heading/H4
      </Typography>
      <Typography variant="heading" type="h5">
        Heading/H5
      </Typography>
      <Typography variant="heading" type="h6">
        Heading/H6
      </Typography>
    </Container>
  )}
</Story>

### Navigation

<Story name="Navigation">
  {(args) => (
    <Container>
      <Typography variant="navigation" type="menu_title">
        Navigation/Menu Title
      </Typography>
      <Typography variant="navigation" type="menu_tabs">
        Navigation/Menu Tabs
      </Typography>
      <Typography variant="navigation" type="label">
        Navigation/Label
      </Typography>
      <Typography variant="navigation" type="drawer_active">
        Navigation/Drawer Active
      </Typography>
      <Typography variant="navigation" type="drawer_inactive">
        Navigation/Drawer Inactive
      </Typography>
      <Typography variant="navigation" type="button">
        Navigation/Button
      </Typography>
      <Typography variant="navigation" type="breadcrumb">
        Navigation/Breadcrumb
      </Typography>
      <Typography variant="navigation" type="breadcrumb_hover">
        Navigation/Breadcrumb
      </Typography>
    </Container>
  )}
</Story>
